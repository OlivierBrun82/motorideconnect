{% extends 'base.html.twig' %}

{% block title %} Créer une balade {% endblock %}

{% block body %}
        <h1>Créer ta balade</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary btn-block">Créer</button>
        {{ form_end(form) }}
   
{% endblock %}